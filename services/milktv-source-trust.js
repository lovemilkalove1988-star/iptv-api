function calculateSourceTrust(source={}){const successes=Math.max(0,Number(source.successful_checks||0)), consecutive=Math.max(0,Number(source.consecutive_successful_checks||0)), failures=Math.max(0,Number(source.failed_checks||0));let score=Math.min(70,successes*5)+Math.min(20,consecutive*4);if(source.status==='online')score+=10;if(source.status==='offline'||source.status==='error')score-=Math.min(45,failures*12);if(source.last_success_at){const age=Date.now()-new Date(source.last_success_at).getTime();if(age>7*86400000)score-=15;else if(age>86400000)score-=5}score=Math.max(0,Math.min(100,Math.round(score)));const level=successes===0?'unknown':score>=70?'trusted':score<35?'unstable':'unproven';return {score,level,reasons:{successes,consecutive,failures}}}
module.exports={calculateSourceTrust};
