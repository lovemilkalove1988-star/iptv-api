const db=require('../database');(async()=>{const q=async s=>(await db.query(s)).rows;const total=(await q('SELECT COUNT(*)::int n FROM milktv_channel_sources'))[0].n;const withP=(await q('SELECT COUNT(DISTINCT source_id)::int n FROM milktv_channel_source_provenance'))[0].n;const multi=(await q('SELECT COUNT(*)::int n FROM (SELECT source_id FROM milktv_channel_source_provenance GROUP BY source_id HAVING COUNT(*)>1)x'))[0].n;const m3u=(await q("SELECT COUNT(*)::int n FROM milktv_channel_source_provenance WHERE m3u_provider_id IS NOT NULL"))[0].n;const disc=(await q("SELECT COUNT(*)::int n FROM milktv_channel_source_provenance WHERE discovery_source_id IS NOT NULL"))[0].n;console.log({total,with_provenance:withP,without_provenance:total-withP,multi_origin:multi,m3u,discovery:disc});await db.end()})().catch(e=>{console.error(e.message);process.exitCode=1});
